package com.kata.testbed.sections.s08_room_persistence.exercises

import androidx.compose.foundation.layout.Column
import androidx.compose.foundation.layout.padding
import androidx.compose.runtime.Composable
import androidx.compose.ui.Modifier
import androidx.compose.ui.unit.dp

@Composable
fun S08E05Exercise() {
    Column(modifier = Modifier.padding(16.dp)) {
        // TODO: Show @Update and @Upsert annotations
        // TODO: Show OnConflictStrategy options (REPLACE, IGNORE, ABORT)
        // TODO: Simulate upsert behavior: insert if new, update if exists
        // TODO: Display before/after state of the data
    }
}
