package com.kata.testbed.sections.s05_lists_layouts.exercises

import androidx.compose.foundation.layout.Column
import androidx.compose.foundation.layout.padding
import androidx.compose.runtime.Composable
import androidx.compose.ui.Modifier
import androidx.compose.ui.unit.dp

@Composable
fun S05E03Exercise() {
    Column(modifier = Modifier.padding(16.dp)) {
        // TODO: Create a map of category -> list of items
        //   e.g., "Fruits" -> listOf("Apple", "Banana", "Cherry")
        // TODO: Create a LazyColumn with Modifier.height(400.dp)
        // TODO: For each category, use stickyHeader { } to show the category name
        // TODO: Use items() to show each item in the category
        // HINT: You need @OptIn(ExperimentalFoundationApi::class)
    }
}
