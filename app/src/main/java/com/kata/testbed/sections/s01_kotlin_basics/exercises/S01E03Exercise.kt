package com.kata.testbed.sections.s01_kotlin_basics.exercises

import androidx.compose.foundation.layout.Column
import androidx.compose.foundation.layout.padding
import androidx.compose.runtime.Composable
import androidx.compose.ui.Modifier
import androidx.compose.ui.unit.dp

@Composable
fun S01E03Exercise() {
    Column(modifier = Modifier.padding(16.dp)) {
        // TODO: Declare val `language` = "Kotlin" and val `version` = 2.0
        // TODO: Display a simple template: "I love $language"
        // TODO: Display an expression template: "${language.length} characters"
        // TODO: Display a multi-expression template: "$language $version is ${if (version >= 2.0) "modern" else "classic"}"
        // TODO: Display a raw string with trimMargin
    }
}
