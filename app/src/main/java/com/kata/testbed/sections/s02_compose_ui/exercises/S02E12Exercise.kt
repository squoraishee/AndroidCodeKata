package com.kata.testbed.sections.s02_compose_ui.exercises

import androidx.compose.foundation.layout.Column
import androidx.compose.foundation.layout.padding
import androidx.compose.runtime.Composable
import androidx.compose.ui.Modifier
import androidx.compose.ui.unit.dp

@Composable
fun S02E12Exercise() {
    Column(modifier = Modifier.padding(16.dp)) {
        // TODO: Define custom light and dark ColorSchemes using lightColorScheme() / darkColorScheme()
        // TODO: Wrap content in MaterialTheme(colorScheme = customColorScheme)
        // TODO: Inside the custom theme, display:
        //   - Text using MaterialTheme.colorScheme.primary
        //   - A Surface with MaterialTheme.colorScheme.surface
        //   - A Button that inherits theme colors
        // TODO: Show how theme colors cascade to child composables
    }
}
