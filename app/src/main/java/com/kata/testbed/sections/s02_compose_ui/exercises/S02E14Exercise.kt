package com.kata.testbed.sections.s02_compose_ui.exercises

import androidx.compose.foundation.layout.Column
import androidx.compose.foundation.layout.padding
import androidx.compose.runtime.Composable
import androidx.compose.ui.Modifier
import androidx.compose.ui.unit.dp

@Composable
fun S02E14Exercise() {
    Column(modifier = Modifier.padding(16.dp)) {
        // TODO: Use BoxWithConstraints to read the available width
        // TODO: If maxWidth > 600.dp, show a two-column layout (Row with two Columns)
        // TODO: If maxWidth <= 600.dp, show a single-column layout
        // TODO: Display the current constraints (maxWidth, maxHeight) as text
        // TODO: Show different content in each layout variant
    }
}
