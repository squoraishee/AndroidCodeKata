package com.kata.testbed.sections.s01_kotlin_basics.exercises

import androidx.compose.foundation.layout.Column
import androidx.compose.foundation.layout.padding
import androidx.compose.runtime.Composable
import androidx.compose.ui.Modifier
import androidx.compose.ui.unit.dp

@Composable
fun S01E12Exercise() {
    Column(modifier = Modifier.padding(16.dp)) {
        // TODO: Create an `object Logger` singleton with a log list and an addLog() function
        // TODO: Create a class `DatabaseConnection` with a `companion object` that has:
        //   - a `create(url: String)` factory method
        //   - a `MAX_CONNECTIONS` constant
        // TODO: Use the Logger singleton to add logs, display the log entries
        // TODO: Use the companion object factory to create an instance, display it
        // TODO: Display the MAX_CONNECTIONS constant
    }
}
