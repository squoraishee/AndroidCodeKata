package com.kata.testbed.sections.s07_dependency_injection.exercises

import androidx.compose.foundation.layout.Column
import androidx.compose.foundation.layout.padding
import androidx.compose.runtime.Composable
import androidx.compose.ui.Modifier
import androidx.compose.ui.unit.dp

@Composable
fun S07E09Exercise() {
    Column(modifier = Modifier.padding(16.dp)) {
        // TODO: Show the @HiltViewModel pattern as text
        // TODO: Create a simulated ViewModel that receives a repository
        // TODO: The ViewModel should expose state from the repository
        // TODO: Display the ViewModel state to show the injection pattern
    }
}
