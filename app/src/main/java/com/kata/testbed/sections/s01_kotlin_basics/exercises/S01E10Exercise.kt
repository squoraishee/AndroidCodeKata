package com.kata.testbed.sections.s01_kotlin_basics.exercises

import androidx.compose.foundation.layout.Column
import androidx.compose.foundation.layout.padding
import androidx.compose.runtime.Composable
import androidx.compose.ui.Modifier
import androidx.compose.ui.unit.dp

@Composable
fun S01E10Exercise() {
    Column(modifier = Modifier.padding(16.dp)) {
        // TODO: Create a data class `Config(var host: String = "", var port: Int = 0, var secure: Boolean = false)`
        // TODO: Use `apply` to configure a Config instance, display it
        // TODO: Use `let` on a nullable value to transform it, display result
        // TODO: Use `run` to compute a value from an object, display result
        // TODO: Use `also` to perform a side effect (add to a log list), display the log
        // TODO: Use `with` to access multiple properties without repeating the object name
    }
}
